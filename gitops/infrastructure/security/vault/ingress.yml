---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: vault-ingress
  namespace: vault
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`vault.nooblearn2code.com`)
      kind: Rule
      services:
        - name: hashicorp-vault
          port: 8200
