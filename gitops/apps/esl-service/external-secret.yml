apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: esl-service-es
spec:
  refreshInterval: "15s"
  secretStoreRef:
    name: shared-vault-backend
    kind: SecretStore
  target:
    name: esl-service-secret
  dataFrom:
    - extract:
        key: esl-service
