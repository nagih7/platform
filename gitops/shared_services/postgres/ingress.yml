apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: shared-postgres-ingress
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`postgres.nooblearn2code.com`)
      kind: Rule
      services:
        - name: shared-postgres-service
          port: 5432
