# ─────────────────────────────────────────────
# SHARED BUILD TEMPLATE
# Include this in your microservice .gitlab-ci.yml:
#
# include:
#   - remote: 'https://raw.githubusercontent.com/nagih7/platform/main/gitops/platform/tooling/gitlab/ci-templates/.base-build.yml'
# ─────────────────────────────────────────────
variables:
  VAULT_ADDR: "http://hashicorp-vault.vault.svc.cluster.local:8200"
  GITOPS_PATH: "gitops/workloads/microservices"

.build_template:
  stage: build
  tags:
    - k8s
  image:
    name: gcr.io/kaniko-project/executor:v1.23.0-debug
    entrypoint: [""]
  id_tokens:
    VAULT_ID_TOKEN:
      aud: "https://gitlab.nooblearn2code.com"
  before_script:
    - |
      echo "🔐 Authenticating to Vault..."

      VAULT_RESPONSE=$(wget -qO- \
        --header "Content-Type: application/json" \
        --post-data "{\"role\":\"ci-build-role\",\"jwt\":\"${VAULT_ID_TOKEN}\"}" \
        "${VAULT_ADDR}/v1/auth/jwt/login")

      VAULT_TOKEN=$(echo "${VAULT_RESPONSE}" | grep -o '"client_token":"[^"]*"' | cut -d'"' -f4)

      if [ -z "$VAULT_TOKEN" ] || [ "$VAULT_TOKEN" = "null" ]; then
        echo "❌ Vault auth failed! Response: ${VAULT_RESPONSE}"
        exit 1
      fi
      echo "✅ Vault token acquired"

      DOCKER_RESPONSE=$(wget -qO- \
        --header "X-Vault-Token: ${VAULT_TOKEN}" \
        "${VAULT_ADDR}/v1/cicd/data/docker")

      DOCKER_USERNAME=$(echo "${DOCKER_RESPONSE}" | grep -o '"username":"[^"]*"' | cut -d'"' -f4)
      DOCKER_PASSWORD=$(echo "${DOCKER_RESPONSE}" | grep -o '"password":"[^"]*"' | cut -d'"' -f4)

      mkdir -p /kaniko/.docker
      echo "{\"auths\":{\"https://index.docker.io/v1/\":{\"auth\":\"$(echo -n "${DOCKER_USERNAME}:${DOCKER_PASSWORD}" | base64 | tr -d '\n')\"}}}" \
        > /kaniko/.docker/config.json

      echo "✅ Docker credentials ready"
  script:
    - |
      IMAGE_SHA="${IMAGE_NAME}:${CI_COMMIT_SHORT_SHA}"
      IMAGE_LATEST="${IMAGE_NAME}:latest"

      echo "🔨 Building ${IMAGE_SHA} with Kaniko..."

      /kaniko/executor \
        --context "${CI_PROJECT_DIR}" \
        --dockerfile "${CI_PROJECT_DIR}/Dockerfile" \
        --destination "${IMAGE_SHA}" \
        --destination "${IMAGE_LATEST}" \
        --cache=true \
        --cache-repo "${IMAGE_NAME}/cache" \
        --no-push-cache \
        --label "git.commit=${CI_COMMIT_SHA}" \
        --label "git.branch=${CI_COMMIT_REF_NAME}" \
        --label "build.date=$(date -u +'%Y-%m-%dT%H:%M:%SZ')"

      echo "✅ Build complete → ${IMAGE_SHA}"

  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
