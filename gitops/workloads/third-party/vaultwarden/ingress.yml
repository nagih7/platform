apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: vaultwarden-ingress
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`bw.nooblearn2code.com`)
      kind: Rule
      services:
        - name: vaultwarden-svc
          port: 80
