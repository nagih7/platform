apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: resumind-es
  namespace: microservices
spec:
  refreshInterval: "15s"
  secretStoreRef:
    name: ms-vault-backend
    kind: SecretStore
  target:
    name: resumind-secret
  dataFrom:
    - extract:
        key: microservices/resumind
