apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: postgres
  namespace: argocd
spec:
  project: default
  source:
    repoURL: "https://github.com/nagih7/homelab-gitops.git"
    targetRevision: main
    path: shared_services/postgres
  destination:
    server: "https://kubernetes.default.svc"
    namespace: shared-services
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
